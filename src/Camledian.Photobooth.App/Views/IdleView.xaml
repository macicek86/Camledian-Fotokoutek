<UserControl x:Class="Camledian.Photobooth.App.Views.IdleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="1280" d:DesignHeight="720">
    <Grid Background="{StaticResource BackgroundBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Camledian Photobooth" Style="{StaticResource ScreenTitle}" FontSize="64" HorizontalAlignment="Center" />
            <TextBlock Text="Dotkněte se obrazovky a začněte" Style="{StaticResource ScreenSubtitle}" HorizontalAlignment="Center" Margin="0,16,0,48" />
            <Button Content="ZAČÍT" Style="{StaticResource KioskButton}" Command="{Binding StartSessionCommand}"
                    HorizontalAlignment="Center" FontSize="40" Padding="64,32" />
        </StackPanel>
    </Grid>
</UserControl>
